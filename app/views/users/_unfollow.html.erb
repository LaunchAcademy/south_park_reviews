<%= form_for(current_user.relationships.find_by(followed_id: @user.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow #{@user.username}", class: "btn btn-large" %>
<% end %>
